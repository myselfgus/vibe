#!/bin/sh
export PATH="$HOME/.bun/bin:$PATH"
# Skip tests in CI/automated environments
if [ -n "$CI" ] || [ -n "$GITHUB_ACTIONS" ]; then
  echo "Skipping tests in CI environment"
  exit 0
fi
bun test
